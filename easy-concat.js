// normally jevabe likhi
const first = 'good';
const last = 'morning';

const full = first + ' ' + last;
console.log(full);

// Another example
const a = 10;
const b = 20;
const result = 'The sum of ' + a + ' and ' + b + ' is ' + (a + b);
console.log(result);

// EAsy version
const first1 = 'good';
const last1 = 'morning';
const full1 = `first last`;
console.log(full);

// Another example
const math = `The sum of ${a} and ${b} is ${a + b}`;
console.log(math);

// line break without \n -- > ` ` er moddhe rekhe just nicher line e likhlei hoye jabe..
const wish = `good
morning`;
console.log(wish);